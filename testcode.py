operators = ["pow", "+", "-", "*", "/", "square", "cube", "mod"]
functions = [power, add, subtract, multiply, divide, square, cube, mod]

for function in functions:
    print function(int(tokens[1], int(tokens[2]))